# =============================================================================
# GitLab Admin Bot - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control - it contains secrets
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   docker compose up -d

# =============================================================================
# Required: GitLab Connection
# =============================================================================
# Create a Personal Access Token with api scope:
#   GitLab -> User Settings -> Access Tokens -> Add new token
#   Scopes: api, read_repository

GITLAB_PRIVATE_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx

# =============================================================================
# Required: Hetzner Cloud API
# =============================================================================
# For automated recovery and restore testing
# Get from: https://console.hetzner.cloud/
#   Project -> Security -> API Tokens -> Generate API Token

HETZNER_API_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# Required: BorgBackup
# =============================================================================
# The passphrase used when initializing the Borg repository
# CRITICAL: Store this securely - without it, backups cannot be restored

BORG_PASSPHRASE=your-secure-borg-passphrase-minimum-20-chars

# =============================================================================
# Required: Email Alerting (SMTP)
# =============================================================================
# For Microsoft 365: use smtp.office365.com:587
# For Gmail: use smtp.gmail.com:587 (requires app password)

SMTP_PASSWORD=your-smtp-password-or-app-password

# =============================================================================
# Optional: Claude AI Integration
# =============================================================================
# Get from: https://console.anthropic.com/
# Leave empty to disable AI-powered analysis

CLAUDE_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# Optional: SSH Key Path
# =============================================================================
# Path to the SSH private key for connecting to GitLab server
# Default: ~/.ssh/admin_bot_key

SSH_KEY_PATH=~/.ssh/admin_bot_key

# =============================================================================
# Optional: Logging
# =============================================================================
# Log level: DEBUG, INFO, WARNING, ERROR
# Debug mode: true/false (enables verbose logging)

LOG_LEVEL=INFO
DEBUG=false
